($a,$b,$c) = (1,79,79);
goto L5 if $a;
goto L9 if 1;
L5: $b *= 100;
$b -= -100000;
while() {
    warn $b;
    ($f,$d) = (1,2);
do {
    $e = 2;
    $f = 0 if not $b % $d;
} while ++$d - $b;
    $h++ unless $f;
    die $h if $b == 124900;
    $b += 17;
}
